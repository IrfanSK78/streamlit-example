# Invasive Outreach Agent Configuration

# Google Workspace Setup
# These are set up via OAuth - no need to add them manually
# Just ensure credentials.json is in the root directory

# Email sender configuration
SENDER_EMAIL="sonia.baig@invasived.com"
SENDER_NAME="Sonia Baig"

# Lead qualification thresholds
MIN_LEAD_SCORE=70

# Optional: Database path (defaults to lead_tracker.db)
# DB_PATH="lead_tracker.db"
